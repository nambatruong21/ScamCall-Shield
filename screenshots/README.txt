Chụp tối đa 5 ảnh màn hình (mục 16 Dev Guide) và đặt vào thư mục này.
Đề xuất:
  1. landing.png    — Hero + thẻ "specimen" (signature) ở nền sáng.
  2. result.png     — Màn hình kết quả: đồng hồ rủi ro, evidence tô màu, kế hoạch ứng phó.
  3. file-scan.png  — Kiểm tra tệp invoice.pdf.exe với các chỉ báo rủi ro.
  4. dark-learn.png — Nền tối + mini-game "Scam hay An toàn?".
  5. model-card.png — Trang "Về model" với số liệu 5-fold CV.

Cách chụp nhanh: chạy `python3 -m http.server` trong web_app/, mở trình duyệt,
dùng phím chụp màn hình của hệ điều hành.
